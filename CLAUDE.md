# CLAUDE.md

이 파일은 이 저장소에서 작업하는 모든 에이전트(Claude Code 포함)의 운영 계약입니다. 무엇을 만드는지, 무엇을 절대 어기지 않는지, 그리고 — 이 프로젝트의 핵심 규율 — **매 변경마다 학습을 어떻게 동기화하는지**를 정의합니다. 길게 풀어 쓴 배경은 `docs/`에 있고, 이 파일은 그 입구이자 규칙입니다.

---

## 1. 프로젝트 한 줄 정의

**OGC 2026 최적화 챌린지** — 다층 블록을 베이에 배치하고 스케줄링해 지각·균형·선호도 목적함수를 최소화하는 문제를 푸는 알고리즘. 제출물은 `src/myalgorithm.py`의 `algorithm(prob_info, timelimit)` 한 함수다.

**목표는 1등이다.** 그러나 1등은 결과일 뿐 방법이 아니다. 이 저장소의 방법은 두 가지다 — (1) **측정 위에서만 결정하고**, (2) **매 변경마다 학습을 문서로 동기화한다.** 리더보드 점수의 상당 부분이 기술 보고서와 발표에서 나오고(참신성 가점 명시), 그 보고서는 우리가 매 단계 남긴 문서에서 그대로 자란다. 그래서 문서 동기화는 부수 작업이 아니라 점수 그 자체다.

---

## 2. 오리엔테이션 — 어디에 무엇이 있나

| 위치 | 역할 |
|---|---|
| `src/` | **제출 패키지(flat layout).** `myalgorithm.py`(진입점), `baseline_greedy.py`, `utils.py`. 여기 있는 파일이 zip 루트로 제출된다. import는 flat 기준(`import baseline_greedy`)으로 쓴다. |
| `baseline/` | 대회 제공 베이스라인 원본(참조용, 수정하지 않음). `src/`의 출발점이자 비교 기준. |
| `alg_tester/` | 크레인 위반을 눈으로 디버깅하는 GUI(`alg_tester_app.py`). 점수 기록·비교용이 아니다. |
| `train/` | 학습 인스턴스(`prob_*.json`). 모든 로컬 측정의 입력. |
| `results/` | 측정 결과(`<name>/summary.csv` + 인스턴스별 솔루션·로그). **gitignore됨** — 숫자는 커밋하지 말고 문서에 인용한다. |
| `docs/` | **지식 베이스.** 아래 4절에서 상세히 다룬다 — 이 프로젝트의 두 번째 산출물. |
| `learning/` | 실험·스크래치 공간. |
| `batch_runner.py` | 서버 모사 측정 도구(병렬 실행, 타임아웃 강제 종료, 회귀 비교). |
| `submit.py` | `src/`를 zip으로 묶어 제출 메일 발송. 형식 검증 내장. |
| `utils.py` | **평가 서버 것으로 덮어써진다.** 로컬에서 읽어 동작을 복제하되, 수정에 의존하는 알고리즘은 금지. |

---

## 3. 핵심 명령

```bash
# 환경 (최초 1회)
conda env create -f ogc2026_env.yml && conda activate ogc2026

# 측정 — 학습 인스턴스 전체를 서버 모사 조건(4코어·16GB·타임아웃 강제)으로 실행
python batch_runner.py run -a src -t 60 --name <run-name>
#   -a 기본값은 src/ ,  -t 제한시간(초),  -i 로 인스턴스 일부 지정 가능

# 비교 — 첫 dir이 기준. 인스턴스별 점수표 + 대회 채점 시뮬레이션 + 회귀 알람
python batch_runner.py compare results/<reference> results/<candidate>

# 제출 — src/ 를 zip으로 묶어 메일 발송 (.env 의 SMTP 설정 필요)
python submit.py
```

GUI 디버깅: `cd alg_tester && python alg_tester_app.py`.

---

## 4. 문서 체계 — `docs/`의 세 층

`docs/`는 세 종류의 글로 이루어지고, 각자 다른 질문에 답한다. 셋은 서로를 링크로 참조하며, 합쳐서 결선 보고서의 뼈대가 된다.

- **`LEARNING_GUIDE.md` — "무엇을 왜 공부하나."** 문제의 구조, 계산기하, 메타휴리스틱의 *배경 지식*. 이론과 출발점. (Part A~F)
- **`STRATEGY_PLAN.md` — "무엇을 어떤 순서로 만들어 이기나."** *의사결정 기록*. 각 결정은 근거(측정)와 종료 기준(완료의 정의)을 함께 가진다. (결정 B1~B6, 단계 P1~P6)
- **`docs/features/` — "구현된 각 기능은 정확히 어떻게 동작하나." ← 이번에 신설하는 학습 동기화의 단위.** 기능 하나당 문서 하나. 코드가 바뀌면 여기가 바뀐다.

세 층의 관계: GUIDE가 *왜 이 길을 공부했는지*를 주고, STRATEGY가 *그 길에서 무엇을 만들기로 했는지*를 결정하고, features가 *실제로 만든 것이 어떻게 동작하는지*를 완결된 형태로 설명한다. 새 기능 문서는 자신이 구현한 결정(STRATEGY Bx)과 배경(GUIDE Part X)을 반드시 역링크한다.

세 층을 가로지르는 공용 참조 세 가지: **`docs/GLOSSARY.md`** — 반복되는 용어(j≥k, stage 1~5, 빈-베이 윈도우, EDD, ALNS …)를 한 번 정의하는 곳. 모든 문서는 용어를 재설명하지 말고 여기로 링크한다. **`docs/INSTANCE_ANALYSIS.md`** — 40개 인스턴스의 원자단위 전수 분석(A3 확장). **`docs/RESULTS.md`** — 흩어진 측정을 모은 결선 보고서의 정량적 척추(전체 개선 호·ablation 표·버린 길·천장·제출 준비 상태). 새 기능을 측정·채택하면 여기 표에 한 줄을 누적한다.

---

## 5. ★ 학습 동기화 규율 — 이 저장소의 제1 작업 규칙

> **코드 변경과 학습 문서 동기화는 한 단위다. 문서가 따라오지 않은 변경은 미완성으로 간주한다.**

의미 있는 기능을 추가·변경·제거할 때마다, 그 기능에 해당하는 `docs/features/` 문서를 신설하거나 갱신한다. 이것이 "올바른 방향성과 성장"을 보장하는 장치다 — 글로 설명되지 않는 기능은 우리가 충분히 이해하지 못한 기능이고, 보고서에 쓸 수 없는 기능이다.

### 5.1 분해 단위 — 기능 하나당 문서 하나

청자(팀원·심사위원·반년 뒤의 나)가 **한 호흡에 이해할 분량**으로 쪼갠다. 대략 1~2 화면. 한 문서가 그보다 커지면 기능이 둘 이상 섞인 것이니 나눈다. 실제 단위의 예(괄호는 배경 위치):

`EDD 디스패칭`(GUIDE B3) · `j≥k 크레인 규칙`(B1) · `검증기 5단계`(B2) · `정수 격자 래스터 엔진`(C2) · `NFP(No-Fit Polygon)`(C3) · `IFP(Inner-Fit Polygon)`(C4) · `BLF 배치`(D1) · `temporal nesting / 층 비대칭`(D3) · `regret-k 구성` · `ALNS destroy/repair`(E2) · `외층 할당 MIP·fix-and-optimize`(F1) · `feasibility-first 래퍼`(B1) · `시간 가드·안정성`(F2).

> 주의: 문서는 **구현된 뒤에** 쓴다. 아직 코드가 없는 기능(예: 현재 NFP·IFP)의 상세 문서를 미리 쓰지 않는다 — 이 저장소는 추측이 아니라 측정·실측 위에 선다. 기능이 착지하는 PR에서 그 문서가 함께 태어난다.

### 5.2 엔지니어링 육하원칙을 산문에 녹인다

각 문서는 아래 여섯 질문에 답한다. **딱딱한 항목 나열이 아니라 흐르는 글 속에 자연스럽게 스며들게** 쓴다(기존 `LEARNING_GUIDE.md`의 문체를 따른다 — 단정하되 근거를 동반하는 산문, 불릿 남발 금지).

1. **왜(Why) — 무엇이 이걸 불렀나.** 어떤 측정·프로파일·실패 모드가 이 기능을 필요하게 만들었는가. 근거는 추측이 아니라 숫자다("Shapely가 프로파일의 50%", "infeasible 11/20" 처럼 인용).
2. **무엇(What).** 이 기능이 정확히 무엇을 하고 무엇을 하지 않는가. 입력과 출력, 책임의 경계.
3. **어떻게(How).** 내부 동작 — 자료구조, 알고리즘, 핵심 수식·불변식. 가능하면 코드를 `file.py:line`으로 가리킨다(클릭 가능).
4. **다른 선택지(Alternatives).** 무엇을 검토했고 무엇을 버렸나, 그리고 그 트레이드오프. 우리가 *고르지 않은 길*을 적는 것이 보고서의 차별화 서사가 된다.
5. **언제·어디서(When/Where).** 코드의 어느 지점, 어느 단계·조건에서 작동하는가. 이 기능이 켜지는·꺼지는 경계.
6. **검증(Who/Verify).** 무엇으로 측정해 채택을 정당화했나 — `batch_runner` 비교표, ablation, 불일치 0건 같은 종료 기준. 회귀 영향과 남은 한계도 함께.

템플릿과 색인은 `docs/features/README.md`와 `docs/features/_TEMPLATE.md`에 있다.

### 5.3 ★ 구현 전 선행 조사 — 바퀴를 다시 깎지 않는다

> **의미 있는 기법을 구현하기 전에, 그 기법의 논문 저자 코드·성숙한 라이브러리·오픈소스가 이미 있는지 먼저 찾는다. 없음을 확인하고서야 직접 짠다.**

SOTA 논문을 읽어 알고리즘을 *이해*하는 것과 그것을 *맞게 구현*하는 것은 다르다. 논문은 핵심 아이디어를 주지만 수치 엣지케이스·퇴화 입력 처리·기본 파라미터·자료구조 선택·수렴 트릭처럼 *구현에서만 드러나는 점*을 대개 생략한다. 그걸 우리가 처음부터 다시 더듬으면 시간을 잃고 미묘한 버그를 들인다. 그래서 어떤 비자명한 기법(NFP·CP 모델·패킹 휴리스틱·메타휴리스틱 등)을 착수하기 전에 순서대로 본다: (1) **논문 저자/대회의 공개 코드**, (2) **성숙한 라이브러리**(ortools의 글로벌 제약, shapely의 기하, scipy 등), (3) **검증된 오픈소스 구현**. 찾으면 *재사용하거나, 최소한 우리 구현을 그 정전(canonical) 구현과 대조해* 놓친 점을 잡는다.

단, 이 저장소만의 제약이 있다: 제출물은 `src/`를 zip 루트로 묶은 flat 패키지(≤15MB)이고 평가 서버에서 돈다. 재사용 후보의 가용 범위는 조직위 FAQ로 넓다(§6 "외부 언어·패키지 허용" 참조) — (1) 평가 환경에 이미 있는 것(ortools·gurobi·xpress·shapely·numpy·numba·cython·torch 등), (2) **zip에 동봉한 파이썬 패키지**(같은 폴더·상대 import), (3) **다른 언어(C++·Rust·Java)로 빌드한 정적 바이너리 + subprocess 호출**. 진짜 제약은 *언어*가 아니라 **서버서 재컴파일·의존성 오류 없이 그대로 도는가 + 15MB**다. 그래서 jagua-rs/sparrow(Rust) 같은 SOTA 엔진도 정전 참조일 뿐 아니라 정적 바이너리로 *실제 탑재 후보*가 된다(서버 OS/아키텍처 호환을 제출 전 확인 못 하니 정적 링크로 리스크 제거 + 실패 시 floor 폴백). 그러니 선행 조사는 "무엇이 SOTA인가"와 "그중 무엇이 *우리 환경에서 실제로 돌아가는가*(벤더링·바이너리 포함)"를 함께 본다. 조사 결과(찾은 구현·왜 채택/기각했는지, 또는 *없어서* 직접 짰다는 사실)는 해당 `docs/features/` 문서의 '어떻게(How)'·'다른 선택지(Alternatives)'에 적는다 — 그 자체가 보고서의 근거가 된다.

### 5.4 변경 완료의 정의 (Definition of Done)

하나의 변경은 다음을 모두 만족해야 "완료"다.

0. **선행 조사를 했다**(§5.3) — 성숙한 구현이 있으면 재사용·대조했고, 없으면 없음을 확인했다.
1. `python batch_runner.py run` 으로 측정했다.
2. `python batch_runner.py compare` 로 기준런 대비 **회귀 0**(feasible→infeasible 퇴보 0건)을 확인했다.
3. 해당 `docs/features/` 문서를 신설·갱신했고, 육하원칙이 글에 들어 있다.
4. 결정이 새로웠다면 `STRATEGY_PLAN.md`에 한 줄(결정 + 왜)을 누적했다.

---

## 6. 절대 규율 — 측정·feasibility·제출

`STRATEGY_PLAN.md`의 결정에서 따라 나오는, 어겨선 안 되는 규칙들:

- **Feasibility-first.** 무슨 일이 있어도 제한시간 안에 feasible한 incumbent를 반환한다. 채점은 인스턴스별 `R − nb`점, infeasible·시간 초과·크래시는 **−1점**. 그래서 평균 최대화보다 **분산 최소화(−1 회피)**가 먼저다. 사후 repair에 의존하지 말고 배치하는 순간에 진입·체류·반출을 검사해 feasible만 만든다.
- **★ train은 표본이지 목표가 아니다 — 과적합 금지.** `train/`의 40개는 채점 대상이 아니다. 점수는 **공개되지 않은 private 인스턴스**에서 매겨지고, 숨김 인스턴스는 공개분과 *비슷한 난이도일 뿐 같지 않다*. 그래서 train 측정은 방향을 잡는 표본일 뿐, 거기에 맞춰 깎으면 private에서 망가진다. 이건 추상적 우려가 아니다 — **v1.0.0이 train 네 게이트를 다 통과하고도 숨김 P3에서 −1을 받았다**(train 커버리지 ≠ 정확성). 그래서 모든 변경은 시니어 엔지니어의 일반화 판단을 거친다: (1) **구조적·일반적 개선**(feasibility 강건성, 불변식 위에 선 연산자, 무회귀 보장)을 train-인스턴스 특정 튜닝보다 우선한다. (2) train 인스턴스 번호·임계·파라미터를 결과에 맞춰 **하드코딩하지 않는다** — 임계가 필요하면 인스턴스 통계(블록수·obj 비중)처럼 *어떤 인스턴스에도 계산되는* 양으로 게이트한다. (3) **소수 인스턴스만 크게 좋아지는** 개선은 과적합을 의심하고, 넓고 안전한 개선을 신뢰한다. (4) 이득이 train-특정이면 private 기대값을 **할인**해서 본다. 요컨대 *최악을 막는 구조*(−1 불가·엄밀개선만)는 일반화하고, *train에 맞춘 첨두*는 private에서 무너진다 — 의심스러우면 후자를 버린다.
- **시간 가드.** 벽시계로 `timelimit × 0.93`에 도달하면 무조건 incumbent를 반환한다. **반환 직전 마지막 `check_feasibility` 비용까지 예산에 포함**한다(베이스라인이 이걸 빠뜨려 시간 초과했다). 전역 try/except로 감싼다. **메인은 감독자다** — 중단 불가능하거나 짧은 제한시간에 overrun하는 무거운 일(구성·ALNS·최종 check)은 종료 가능한 **`os.fork()` 자식**이 하고, 메인은 floor를 손에 쥔 채 timeout-bounded로 수집만 한다(features/11). ★주의: `multiprocessing` 기반 supervisor(v1.0.1/v1.1.0)는 daemon 서버서 fork가 막혀 무력했다 — `os.fork()`(v1.1.1)가 P3 −1의 *진짜* 근본 수정이다(위 서버 실행환경 참조).
- **측정으로만 결정.** 모든 개선 주장은 `batch_runner` 표를 동반해야 인정된다. 의사결정 지표는 평균이 아니라 **순위 시뮬레이션 점수**. 단 위 과적합 금지에 따라, 순위가 *소수 인스턴스의 우연한 변동*(ALNS 벽시계 노이즈 ±1~2%)에서 오는지 *넓은 구조적 이득*에서 오는지 구분한다.
- **무회귀.** 단 한 인스턴스라도 feasible→infeasible로 퇴보하면 채택하지 않는다.
- **제출 규율.** 승인된 제출 후 **12시간 쿨다운**. 학습 인스턴스 전체 × 네 제한시간(10·60·300·1800초)에서 **invalid 0건**을 확인하기 전에는 제출 금지. 제출 전 점검은 GUIDE Part F2.
- **★ 제출 피드백은 점수·순위를 주지 않는다 — feasibility 수와 목적값만.** 따라서 *우리 순위는 영영 알 수 없고*, 채점(인스턴스별 R−nb)이 목적값에 단조(낮은 obj = 낮은 순위, 타팀 고정 시)이므로 **우리 목적값 최소화가 유일한 신호**다. 결정은 "피드백으로 순위 파악"을 기다릴 수 없다 — train 목적값 측정이 의사결정 기준이고(과적합 금지 하에), 제출 피드백은 *hidden feasibility·스케일 확인*과 *우리 obj가 실제로 내려갔는지* 검증에만 쓴다.
- **★ 서버 실행환경(이번에 확정).** 평가 서버는 `algorithm()`을 **daemon 프로세스**에서 실행한다 → `multiprocessing.Process`는 'daemonic processes can't have children'으로 막힌다. 자식 병렬은 **raw `os.fork()`**로만 된다(features/11, v1.1.1 P3 −1 근본수정). CPU는 **firejail+cpulimit 400% throttling**(코어 보이나 총량 4, 초과 시 종료 아니라 *느려질 뿐*) — `worker=4` 권장, oversubscription은 CP-SAT를 굶긴다(nw=5가 prob_38 +11.6%). multiprocessing·멀티스레드는 4코어 내 허용. **로컬 서버 모사는 반드시 `systemd-run --user --scope -p CPUQuota=400%` + 1개씩**(동시 실행은 N배 oversubscription 아티팩트, `learning/THROTTLE_MEASUREMENT.md`).
- **제출 형식.** `src/myalgorithm.py`가 zip 루트, 함수 시그니처 `algorithm(prob_info, timelimit)` 고정, zip ≤ 15MB, 좌표·시각은 정수 출력, 같은 시점에선 EXIT가 ENTRY보다 앞. `utils.py`는 서버 것으로 덮어써지므로 수정 의존 금지. (`submit.py`가 일부를 자동 검증한다.)
- **★ 외부 언어·패키지 허용(조직위 FAQ 확정).** 두 가지가 열려 있다. (1) **다른 언어(C++·Rust·Java 등)로 만든 라이브러리/실행파일을 zip에 넣고 `myalgorithm.py`에서 `subprocess` 등으로 호출**할 수 있다 — 단 평가 서버에서 **추가 컴파일·의존성 오류 없이 그대로 실행**돼야 한다(정적 링크·자기완결 바이너리, 서버에 없는 동적 라이브러리 의존 금지). (2) **환경에 없는 파이썬 패키지**도 **모든 의존성을 zip에 동봉**하면 쓸 수 있다 — `myalgorithm.py`와 같은 폴더에 두고 상대 경로로 import, zip 전체 ≤ 15MB. ⇒ §5.3의 "이식 가능"은 *순수 파이썬*에 한정되지 않는다: **사전 컴파일 바이너리(어느 언어든) + subprocess**, **벤더링한 파이썬 패키지** 모두 후보다. 제약은 언어가 아니라 *재컴파일·의존성 없이 서버서 도는가* + *15MB*. 따라서 jagua-rs/sparrow(Rust) 같은 SOTA 엔진도 정적 바이너리로 빌드해 호출 가능 — ★단 (a) 서버 OS/아키텍처(glibc 버전 등) 호환을 *제출 전 확인 불가*하니 정적 링크로 리스크를 없애고, (b) subprocess 호출 비용·`os.fork()` 감독자 구조(위 "서버 실행환경" 항목)와의 정합을 측정으로 검증한 뒤에만 채택한다(feasibility-first: 바이너리 실패 시 floor로 폴백). **★(c) 동봉 바이너리는 런타임에 `os.chmod(0o755)`로 실행권한을 *반드시 복원*한다** — 평가 서버가 제출 zip을 Python `zipfile.extractall`로 풀면 바이너리가 0o644로 추출돼(+x 벗겨짐, zipfile의 알려진 동작) `os.access(X_OK)=False`로 C 엔진이 *통째로 폴백*한다. 이게 v1.3.0 제출이 서버서 효과 0이었던 근본 원인이다(commit 5c64068). **★(d) 바이너리 검증은 반드시 zip을 *`zipfile`로* 풀어 그 dir서 실행해 확인한다** — `unzip` CLI나 `-a src` 직접 실행은 +x를 보존하므로 서버 조건을 재현하지 못한다(이 허점이 무력한 v1.3.0을 통과시켰다).

---

## 7. 글쓰기 voice (문서 전반)

기존 `docs/`의 문체를 유지한다. 흐르는 한국어 산문, 단정하되 항상 근거(측정·인용)를 동반, 불릿보다 문단. 숫자는 `results/`에서 인용하되 `results/`를 커밋하지는 않는다. 코드는 `file.py:line`으로 가리켜 클릭 가능하게.

**청자를 먼저 생각한다.** 독자는 최적화는 알지만 *이 문제*는 모르는 팀원·심사위원·반년 뒤의 나다. 그래서 (1) 기능 문서는 맨 위 "한눈에" 요약으로 *무엇을·왜*를 먼저 주고 그다음 깊이로 들어간다. (2) 문제 특화 용어는 절대 설명 없이 던지지 않는다 — 반복 용어는 `GLOSSARY.md`에 한 번 정의하고 `[용어](../GLOSSARY.md)`로 링크하며, 그 문서만의 용어는 첫 등장에서 한 문장으로 풀어 쓴다. 직관(왜 이게 통하나)을 먼저, 정밀한 정의를 나중에. 용어집에 없는 새 반복 용어가 생기면 그때 `GLOSSARY.md`에 추가한다.

**사람이 쓴 글처럼 다듬는다.** 문서를 새로 쓰거나 크게 고친 뒤에는 산문을 **humanize-korean** 플러그인으로 윤문해 AI 티를 걷어낸다 — `/humanize-korean` 스킬을 호출하거나, 번들 에이전트가 미등록(`/reload-plugins` 필요)이면 그 룰북 `references/quick-rules.md`를 직접 적용한다. 주로 걷어낼 패턴: em-dash 부연 남발, 결산 피벗구(결론적으로·이를 통해·시사하는 바가 크다), "~것이다/~라는 의미다" 결말, "~할 수 있다" 단정 회피 남발, 번역투("~에 대해·~를 통해·~에 기반하여"), 문두 접속사 과다, "~다" 종결 단조. 단, **수치·고유명사·`코드`·`[링크](경로)`·표·마크다운 구조는 절대 건드리지 않는다**(Do-NOT) — 윤문은 산문의 리듬·표현만 바꾸고 의미·구조는 보존한다. 보고서와 발표가 최종 점수의 상당 부분이므로, 문서가 사람 글로 읽히는 것 자체가 점수다.

---

## 8. 빠른 용어 색인 (→ 배경 위치)

j≥k 크레인 규칙 → GUIDE B1 · 검증기 5단계 → B2 · EDD/MDD/ATC 디스패칭 → B3 · AABB 후보점의 한계 → B4 · 래스터 엔진 → C2 · NFP → C3 · IFP → C4 · BLF → D1 · temporal nesting → D3 · ALNS → E2 · 수리 최적화·MIPStart → F1 · 시간 관리 → F2. 결정과 단계(B1~B6, P1~P6)는 `STRATEGY_PLAN.md`.
